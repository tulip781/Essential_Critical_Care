<div class="sub-navbar">
  <div class="left-links-sub-nav">
    <%= icon('fas', 'less-than') %>
    <span class="subnav-safety-wrap">
      <%= link_to "#{t('home.subnav_backto')} #{@sub_category.category.title}", category_path(@sub_category.category), class: "sub-nav-home-text" %>
    </span>
  </div>
  <%= icon('fas', 'bars') %>
</div>

<%= cl_image_tag(@sub_category.category.navbar_base_folder.banner.key, width: 200, quality: 10) %>
<h5><%= @sub_category.category.navbar_base_folder.title %></h5>
<h5><%= link_to @sub_category.category.title, category_path(@sub_category.category) %></h1>
<h1><%= @sub_category.title %></h1>
<p><%= @sub_category.created_at %></p>
<p><%= @sub_category.description %></p>



<%= render partial: "render_resources", locals: { resources: @all_resources } %>

